export declare type ObservableListener<T> = (value: T) => any;
export declare type ObservableUnsubscribeFn = () => void;
export declare class Observable<T> {
    protected listeners: Array<ObservableListener<T>>;
    subscribe(listener: ObservableListener<T>): ObservableUnsubscribeFn;
    notify(value: T): void;
}
