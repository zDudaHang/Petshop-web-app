import { Interpolation } from 'emotion';
import React from 'react';
import { Theme } from '../../styles';
import { Icons } from '../Icon';
export declare type AlertType = 'info' | 'success' | 'warning' | 'danger';
export interface AlertProps extends React.HTMLAttributes<HTMLDivElement> {
    type: AlertType;
    onCloseClick?: any;
    inline?: boolean;
    styles?: {
        wrapper?: Interpolation;
        container?: Interpolation;
    };
}
export declare function Alert(props: AlertProps): JSX.Element;
export declare const createStyles: (theme: Theme, { inline }: AlertProps) => {
    wrapper: React.CSSProperties;
    container: React.CSSProperties;
    icon: React.CSSProperties;
    content: React.CSSProperties;
    closeButtonWrapper: React.CSSProperties;
    closeButton: React.CSSProperties;
};
export declare const createTypesStyles: (theme: Theme) => {
    danger: {
        icon: Icons;
        style: Interpolation;
    };
    success: {
        icon: Icons;
        style: Interpolation;
    };
    info: {
        icon: Icons;
        style: Interpolation;
    };
    warning: {
        icon: Icons;
        style: Interpolation;
    };
};
