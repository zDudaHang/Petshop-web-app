import React from 'react';
import { ExternalStyles, Theme } from '../../styles';
export interface TooltipPopperProps {
    text: string;
    style?: ExternalStyles;
}
export declare function TooltipPopper(props: TooltipPopperProps): JSX.Element;
export declare const styles: (theme: Theme) => {
    base: React.CSSProperties;
};
export declare const sizeStyles: () => {
    small: React.CSSProperties;
    big: React.CSSProperties;
};
