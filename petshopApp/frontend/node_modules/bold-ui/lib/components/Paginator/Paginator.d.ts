import React from 'react';
import { Theme } from '../../styles';
export interface PaginatorProps {
    /**
     * Current page. 0-indexed.
     */
    page: number;
    /**
     * Total number of pages.
     */
    total: number;
    /**
     * Called when the current page is changed.
     */
    onChange?(page: number): void;
}
export declare function Paginator(props: PaginatorProps): JSX.Element;
export declare namespace Paginator {
    var defaultProps: Partial<PaginatorProps>;
}
export declare const createStyles: (theme: Theme) => {
    paginator: React.CSSProperties;
    disabled: React.CSSProperties;
    input: React.CSSProperties;
    leftButton: React.CSSProperties;
    rightButton: React.CSSProperties;
};
