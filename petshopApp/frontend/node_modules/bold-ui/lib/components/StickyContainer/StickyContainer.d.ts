import { PositionProperty } from 'csstype';
import React from 'react';
import { ExternalStyles, Theme } from '../../styles';
import { Omit } from '../../util';
export interface StickyContainerProps extends Omit<React.HTMLAttributes<HTMLDivElement>, 'style'> {
    top?: number;
    left?: number;
    style?: ExternalStyles;
}
export declare function StickyContainer(props: StickyContainerProps): JSX.Element;
export declare const createStyles: (theme: Theme, { left, top }: StickyContainerProps, position: PositionProperty) => {
    container: React.CSSProperties;
};
