import React from 'react';
import { ExternalStyles, Theme } from '../../../styles';
export interface SelectMultiItemProps {
    style?: ExternalStyles;
    disabled?: boolean;
    children?: React.ReactNode;
    onRemove(e: React.MouseEvent<HTMLSpanElement>): void;
}
export declare function SelectMultiItem(props: SelectMultiItemProps): JSX.Element;
export declare const createStyles: (theme: Theme, { disabled }: SelectMultiItemProps) => {
    root: React.CSSProperties;
    text: React.CSSProperties;
    button: React.CSSProperties;
};
