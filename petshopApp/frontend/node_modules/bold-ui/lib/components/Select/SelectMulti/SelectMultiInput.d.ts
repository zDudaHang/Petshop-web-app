import React from 'react';
import { Theme } from '../../../styles';
import { Omit } from '../../../util';
import { InputProps } from '../../Input/Input';
export interface SelectMultiInputProps<T> extends Omit<InputProps, 'style'> {
    wrapperRef?: React.RefObject<HTMLDivElement>;
    items: T[];
    invalid?: boolean;
    clearable?: boolean;
    renderItem(item: T): React.ReactNode;
    onRemoveItem(item: T): void;
}
export declare function SelectMultiInput<T>(props: SelectMultiInputProps<T>): JSX.Element;
export declare const createStyles: (theme: Theme, { items, disabled }: SelectMultiInputProps<any>) => {
    wrapper: React.CSSProperties;
    disabled: React.CSSProperties;
    invalid: React.CSSProperties;
    item: React.CSSProperties;
    input: React.CSSProperties;
};
