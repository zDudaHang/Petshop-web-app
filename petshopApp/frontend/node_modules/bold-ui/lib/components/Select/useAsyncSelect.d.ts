/**
 * Function that is used to populate the select menu.
 *
 * @param filter The current select input filter.
 * @return A promise of the items to be populated.
 */
export declare type AsyncSelectLoadFn<T> = (filter: string) => Promise<T[]>;
export declare function useAsyncSelect<T>(loadItems: AsyncSelectLoadFn<T>, debounceMs?: number): {
    getSelectProps: () => {
        items: T[];
        loading: boolean;
        onFilterChange: (filter: string) => void;
    };
};
