import React from 'react';
import { ExternalStyles } from '../../styles';
import { Omit } from '../../util';
export interface StepperProps extends Omit<React.HTMLAttributes<HTMLDivElement>, 'style'> {
    style?: ExternalStyles;
    overrides?: {
        Root?: React.ElementType;
    };
}
export declare function Stepper(props: StepperProps): JSX.Element;
export declare namespace Stepper {
    var defaultProps: Partial<StepperProps>;
}
export declare const defaultComponents: StepperProps['overrides'];
