import { StepProps } from './Step';
export declare function useStepperState(initialCurrStep?: number): {
    getStepProps: (step: number) => StepProps;
    currentStep: number;
    setCurrentStep: import("react").Dispatch<import("react").SetStateAction<number>>;
    nextStep: () => void;
    previousStep: () => void;
};
/**
 * Create a StepProps properties object to pass to a Step component, based on its index and current Stepper state.
 *
 * @param step The step index that will receive the props.
 * @param currentActiveStep The current active stepper step.
 */
export declare function getStepProps(step: number, currentActiveStep: number): StepProps;
