import React from 'react';
import { ExternalStyles, Theme } from '../../styles';
import { Omit } from '../../util';
export interface ModalContainerProps extends Omit<React.HTMLAttributes<HTMLDivElement>, 'style'> {
    style?: ExternalStyles;
    hasCloseIcon?: boolean;
    onClose?(): any;
}
export declare function ModalContainer(props: ModalContainerProps): JSX.Element;
export declare namespace ModalContainer {
    var defaultProps: Partial<ModalContainerProps>;
}
export declare const styles: (theme: Theme) => {
    wrapper: React.CSSProperties;
    closeButton: React.CSSProperties;
};
