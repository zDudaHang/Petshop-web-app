import { Observable } from '../../../util/Observable';
export interface ModalStoreItem {
    key: string;
    component: React.ReactNode;
}
export interface ModalStoreState {
    items: ModalStoreItem[];
}
export interface ModalStoreAppendProps {
    dispose(): void;
}
export declare type ModalStoreRenderFn = (props: ModalStoreAppendProps) => React.ReactNode;
export declare class ModalStore extends Observable<ModalStoreState> {
    private value;
    notify(value: ModalStoreState): void;
    append(render: ModalStoreRenderFn): string;
    private dispose;
}
declare const _default: ModalStore;
export default _default;
