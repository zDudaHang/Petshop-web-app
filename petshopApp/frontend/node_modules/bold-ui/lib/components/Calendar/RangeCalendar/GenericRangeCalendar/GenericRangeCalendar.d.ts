import { Theme } from '../../../../styles';
import { CalendarProps } from '../../Calendar';
export interface GenericRangeCalendarProps extends CalendarProps {
    startDate: Date;
    endDate: Date;
    isInTheRange(day: Date): boolean;
    isInTheHoverRange(day: Date, hoverDate: Date): boolean;
}
export declare function GenericRangeCalendar({ isInTheRange, isInTheHoverRange, startDate, endDate, ...rest }: GenericRangeCalendarProps): JSX.Element;
export declare const dayHoverStyle: (theme: Theme) => {
    background: string;
};
