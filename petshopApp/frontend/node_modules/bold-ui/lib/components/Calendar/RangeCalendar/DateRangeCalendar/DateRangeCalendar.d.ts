import { Theme } from '../../../../styles';
import { CalendarProps } from '../../Calendar';
import { DateRange } from '../../../DateRangeField';
export interface DateRangeCalendarProps extends CalendarProps {
    value: DateRange;
    minDate?: Date;
    maxDate?: Date;
    inputOnFocus: number;
}
export declare function DateRangeCalendar({ value, inputOnFocus, maxDate, minDate, ...rest }: DateRangeCalendarProps): JSX.Element;
export declare const dayHoverStyle: (theme: Theme) => {
    background: string;
};
