import { Placement } from '@popperjs/core';
import { ReferenceMonth } from '../MonthPicker/MonthPicker';
export interface MonthPaginatorProps {
    month?: number;
    year?: number;
    isOpen?: boolean;
    ghost?: boolean;
    popperPlacement?: Placement;
    formatter?: (date: Date, month: Intl.DateTimeFormat) => string;
    onChange?(referenceMonth: ReferenceMonth): any;
}
export declare function MonthPaginator(props: MonthPaginatorProps): JSX.Element;
export declare namespace MonthPaginator {
    var defaultProps: {
        isOpen: boolean;
        popperPlacement: string;
        month: number;
        year: number;
    };
}
