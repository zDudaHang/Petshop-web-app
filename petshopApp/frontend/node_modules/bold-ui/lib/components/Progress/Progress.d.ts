import React from 'react';
import { ExternalStyles, TextColor } from '../../styles';
import { Theme } from '../../styles/theme/createTheme';
import { Omit } from '../../util';
export declare type ProgressType = TextColor;
export interface ProgressProps extends Omit<React.ProgressHTMLAttributes<HTMLProgressElement>, 'style'> {
    type?: ProgressType;
    style?: ExternalStyles;
}
export declare function Progress(props: ProgressProps): JSX.Element;
export declare namespace Progress {
    var defaultProps: Partial<ProgressProps>;
}
export declare const createStyles: (theme: Theme, { type }: ProgressProps) => {
    progress: React.CSSProperties;
};
