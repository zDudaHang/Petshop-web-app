import React from 'react';
import { ExternalStyles } from '../../styles';
import { Omit } from '../../util';
import { AlignItems, JustifyContent } from '../Grid';
export interface HFlowProps extends Omit<React.HTMLAttributes<HTMLDivElement>, 'style'> {
    hSpacing?: number;
    alignItems?: AlignItems;
    justifyContent?: JustifyContent;
    style?: ExternalStyles;
}
export declare function HFlow(props: HFlowProps): JSX.Element;
export declare namespace HFlow {
    var defaultProps: HFlowProps;
}
