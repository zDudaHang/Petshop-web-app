import React from 'react';
import { ButtonProps } from '../Button';
import { Icons } from '../Icon/generated/types';
export interface InputWrapperProps {
    icon?: Icons;
    iconPosition?: 'left' | 'right';
    iconDisabled?: boolean;
    clearVisible?: boolean;
    onIconClick?: ButtonProps['onClick'];
    children?: React.ReactNode;
    onClear?(e: React.MouseEvent<HTMLButtonElement>): any;
}
export declare function InputWrapper(props: InputWrapperProps): JSX.Element;
export declare namespace InputWrapper {
    var defaultProps: Partial<InputWrapperProps>;
}
