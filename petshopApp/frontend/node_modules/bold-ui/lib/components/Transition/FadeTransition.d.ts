import React from 'react';
import { TransitionProps, TransitionStatus } from 'react-transition-group/Transition';
import { ClassNames } from '../../styles';
export declare type FadeTransitionProps = Partial<TransitionProps> & {
    children?(renderProps: FadeTransitionRenderProps): React.ReactNode;
};
export interface FadeTransitionRenderProps {
    state: TransitionStatus;
    classes: ClassNames<'default' | 'entering' | 'entered'>;
    className?: string;
}
export declare function FadeTransition(props: FadeTransitionProps): JSX.Element;
export declare namespace FadeTransition {
    var defaultProps: {
        timeout: number;
    };
}
