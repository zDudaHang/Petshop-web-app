export interface TableSizeDropdownProps {
    size: number;
    options: number[];
    onChange(size: number): any;
}
export declare function TableSizeDropdown(props: TableSizeDropdownProps): JSX.Element;
