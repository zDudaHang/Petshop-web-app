import { CSSProperties } from 'react';
import { Theme } from '../../../styles';
export declare const createTableStyles: (theme: Theme) => {
    table: CSSProperties;
    tableHover: CSSProperties;
    thead: CSSProperties;
    tbody: CSSProperties;
    row: CSSProperties;
    th: CSSProperties;
    cell: CSSProperties;
    pointer: CSSProperties;
};
